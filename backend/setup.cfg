[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Fast domain logic tests
    integration: Layer integration tests
    slow: Slow tests requiring external services
